extends layout.pug
block content
  section(class='items')
    
    div(class='category-info')
      h1(class='category-name') #{categoryData[0].name}
      h2 #{categoryData[0].description}
    ul(class='main-container')
      each item in categoryItems  
        a(href=item.url)
          li(class='item-container') 
            img(src='/images/placeholder.png', width='100%', height='100%', alt='placeholder image')
            div(class='item-details')
              div(class='price') #{item.priceString}
              div(class='name') #{item.name}
  