extends layout.pug
block content
  section(class='items')
    p #{categoryData[0].name}
    p #{categoryData[0].description}
    
    ul
    each item in categoryItems  
      li 
        a(href=item.url) #{item.name}
  